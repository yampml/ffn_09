<% provide :title, @league.name %>
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header"><%= image_tag @league.logo.url if @league.logo? %><%= @league.name %></h1>
    </div>
  </div>
  <div class="row">
    <br>
    <%= render partial: "teams/team", collection: @league.teams %>
  </div>
</div>
